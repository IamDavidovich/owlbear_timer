/** Get the reverse domain name id for this plugin at a given path */
export function getPluginId(path: string) {
    https://iamdavidovich.github.io/owlbear_timer/
    return `io.github.iamdavidivich.owlbear_timer/${path}`;
}
